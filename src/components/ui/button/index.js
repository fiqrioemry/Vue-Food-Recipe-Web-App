import { cva } from "class-variance-authority";

export { default as Button } from "./Button.vue";

export const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium disabled:cursor-not-allowed disabled:bg-button-disabled disabled:pointer-events-none min-w-[100px] transition-colors duration-300 ",
  {
    variants: {
      variant: {
        default: "bg-button text-background  hover:opacity-80",
        secondary:
          "bg-foreground text-primary shadow-sm hover:bg-button hover:text-white",
        ghost: "hover:bg-accent hover:text-accent-foreground",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-9 px-4 py-2",
        sm: "h-8 rounded-md px-3 text-xs",
        lg: "h-12 rounded-md px-6 py-6",
        icon: "h-9 w-9",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
);
